# TTNN notebook tutorial test.

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "notebook_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    tags = ["ttnn", "notebook"],
)
