# TTNN Tracy profiler tests (4 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "tracy_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
    ],
    tags = ["ttnn", "tracy", "profiling"],
)
