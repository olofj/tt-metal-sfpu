# Nightly data movement operation tests (5 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_data_movement_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//tests/ttnn:test_utils",
        "//tests/ttnn/unit_tests/operations:test_utils",
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "data_movement"],
)
