# Nightly reduction operation tests (1 test file).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_reduction_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "reduction", "requires_wormhole_b0"],
)
