# Nightly transformer operation tests (3 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_transformers_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//tests/tt_eager:sweep_test_utils",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "transformers", "requires_wormhole_b0"],
)
