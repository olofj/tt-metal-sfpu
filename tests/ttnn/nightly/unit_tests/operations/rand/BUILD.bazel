# Nightly random operation tests (3 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_rand_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "rand"],
)
