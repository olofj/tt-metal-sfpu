# Nightly matmul operation tests (2 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_matmul_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//tests/ttnn:test_utils",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//numpy",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "matmul", "requires_wormhole_b0"],
)
