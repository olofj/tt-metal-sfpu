# Nightly experimental operation tests (3 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "nightly_experimental_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "experimental"],
)
