# Nightly T3000 2D CCL tests (2 test files).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "t3000_2d_ccl_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//tests/nightly/t3000/ccl:ccl_test_modules",
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "ccl", "t3000", "2d_ccl", "requires_wormhole_b0", "requires_T3000"],
)
