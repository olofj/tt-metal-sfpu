# Nightly RetinaNet model test.

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "retinanet_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//ttnn/ttnn:ttnn_py",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "single_card", "requires_wormhole_b0"],
)
