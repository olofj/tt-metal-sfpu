# GPT-OSS accuracy tests.
# Referenced by: t3k_demo_tests, galaxy_demo_tests.

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "gpt_oss_accuracy_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0", "requires_T3000"],
)
