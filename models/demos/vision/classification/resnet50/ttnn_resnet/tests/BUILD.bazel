# ResNet50 model tests.
# Referenced by: t3k_demo_tests, t3k_perf_tests, t3k_integration_tests,
#   galaxy_integration_tests (resnet50).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "resnet50_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//tests/ttnn:test_utils",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//torchvision",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0", "requires_blackhole"],
)
