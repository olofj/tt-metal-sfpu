# DeepSeek v3 fused op unit tests.

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "deepseek_v3_fused_op_tests",
    srcs = glob(["*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0"],
)
