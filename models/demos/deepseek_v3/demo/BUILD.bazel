# DeepSeek v3 demo tests.
# Referenced by: galaxy_demo_tests (deepseek tg_stress).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "deepseek_v3_demo_tests",
    srcs = glob(["test_*.py"]),
    data = glob([
        "*.json",
        "*.refpt",
    ]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0"],
    timeout_seconds = 600,
)
