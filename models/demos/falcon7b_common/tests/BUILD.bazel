# Falcon-7B common model tests.
# Referenced by: t3k_perf_tests (falcon7b perf).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "falcon7b_common_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0"],
)
