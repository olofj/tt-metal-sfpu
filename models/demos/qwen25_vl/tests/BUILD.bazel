# Qwen2.5-VL model tests.
# Referenced by: t3k_unit_tests (qwen25_vl unit tests).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "qwen25_vl_tests",
    srcs = glob(
        ["test_*.py"],
        exclude = ["test_ci_dispatch.py"],
    ),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0", "requires_T3000"],
)
