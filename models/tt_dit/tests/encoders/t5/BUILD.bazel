# T5 encoder tests for DiT models.
# Referenced by: t3k_unit_tests (dit encoder tests).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "t5_encoder_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "dit", "requires_wormhole_b0"],
)
