# Mochi video generation model tests.
# Referenced by: t3k_demo_tests, t3k_integration_tests, t3k_perf_tests,
#   galaxy_demo_tests, galaxy_integration_tests, galaxy_perf_tests.

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "mochi_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "dit", "requires_wormhole_b0"],
)
