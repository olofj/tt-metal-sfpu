# Functional UNet model tests.
# Referenced by: t3k_unit_tests (unet shallow).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "functional_unet_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0"],
)
