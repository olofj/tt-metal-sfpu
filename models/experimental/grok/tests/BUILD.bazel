# Grok model tests.
# Referenced by: t3k_unit_tests (grok).

load("//bazel:pytest.bzl", "pytest_suite")

pytest_suite(
    name = "grok_tests",
    srcs = glob(["test_*.py"]),
    deps = [
        "//models/common:utility_functions",
        "//ttnn/ttnn:ttnn_py",
        "@pip//loguru",
        "@pip//torch",
        "@pip//transformers",
    ],
    markers = ["slow"],
    tags = ["ttnn", "nightly", "models", "requires_wormhole_b0", "requires_T3000"],
)
