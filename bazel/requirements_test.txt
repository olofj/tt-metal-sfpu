# Python test dependencies for Bazel py_test targets.
# Locked versions match tt_metal/python_env/requirements-dev.txt.
#
# These packages are resolved by rules_python pip.parse() and made available
# as @pip//<package> deps in BUILD files.
#
# To regenerate the lock file after editing this file:
#   pip-compile --allow-unsafe --strip-extras --no-emit-index-url --no-emit-options \
#     --extra-index-url https://download.pytorch.org/whl/cpu \
#     --output-file=bazel/requirements_test_lock.txt \
#     bazel/requirements_test.txt

pytest==8.4.2
pytest-timeout==2.4.0
pytest-split==0.10.0

# Core numeric/ML stack used by nearly all tests.
# CPU-only torch to avoid downloading CUDA deps (matches requirements-dev.txt).
numpy>=1.24.4,<2
torch==2.7.1
loguru==0.6.0

# ML model dependencies used by model tests.
transformers==4.53.0
torchvision==0.22.1
pandas==2.3.3

# Additional ML dependencies for model tests (Phase 2).
# Pins match tt_metal/python_env/requirements-dev.txt where available.
scipy>=1.11
diffusers==0.35.1
datasets==2.9.0
timm>=1.0.0
evaluate==0.4.0

# Test utilities
pyyaml>=5.4
graphviz>=0.20.3

# Documentation build (Sphinx + extensions).
# Used by //docs:html_docs target.
# Pin bleach<6 — bleach 6.x removed CSS sanitization that nbconvert requires.
bleach>=5,<6
# Pin nbconvert<7.17 — 7.17.0 has a bug where ALLOWED_STYLES is undefined
# when the CSS sanitizer is not fully initialized (NameError in sanitize.py).
nbconvert>=7.16,<7.17
sphinx==7.1.2
sphinx-rtd-theme==1.3.0
sphinxcontrib-email==0.3.5
breathe==4.35.0
nbsphinx==0.9.3
sphinxcontrib-jquery==4.1
myst-parser==3.0.0
lxml==4.9.4
docutils==0.18.1
tabulate==0.9.0
